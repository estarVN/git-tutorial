https://www.youtube.com/watch?v=cC7vg8lWneo

working folder ========> stage area =======> local repositories ========> remote repositories

install git 
https://git-scm.com/downloads

--or create a new repository on the command line
echo "# test" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/estarVN/test.git
git push -u origin main

-- or push an existing repository from the command line
git remote add origin https://github.com/estarVN/test.git
git branch -M main
git push -u origin main

git config --global user.name 'EstarVn'
git config --global user.email 'estar@gmail.com'
git config --global pull.rebase false

git init
git add filename1 filename2
git add .
git status
git commit -m "comment of this commit"